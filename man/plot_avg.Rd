% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_avg.R
\name{plot_avg}
\alias{plot_avg}
\title{Plot aggregated (average) comparisons and slopes}
\usage{
plot_avg(x, ...)
}
\arguments{
\item{x}{object produced by the \code{comparisons()} or \code{slopes()} object.}

\item{...}{additional arguments are passed to the \code{aggregate} function (e.g., \code{conf_level}).}
}
\value{
A \code{ggplot2} object
}
\description{
\code{x} is re-evaluated by the corresponding \verb{avg_*()} function the result is drawn as a point-range plot using \code{ggplot2}.
}
\examples{
mod <- glm(am ~ hp + wt, data = mtcars)
mfx <- slopes(mod)
plot_avg(mfx)


mod <- glm(am ~ hp + factor(gear), data = mtcars)
cmp <- comparisons(mod)
plot_avg(cmp)
}
\seealso{
Other plot: 
\code{\link{plot_cap}()},
\code{\link{plot_cco}()},
\code{\link{plot_cme}()}
}
\concept{plot}
