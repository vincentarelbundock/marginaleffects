
Take a <code>summary()</code> style <code>vcov</code> argument and convert it to
<code>insight::get_varcov()</code>

### Description

Take a <code>summary()</code> style <code>vcov</code> argument and convert it to
<code>insight::get_varcov()</code>



### Usage

<pre><code class='language-R'>get_varcov_args(model, vcov)
</code></pre>

