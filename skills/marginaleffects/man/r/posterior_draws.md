
## alias to <code>get_draws()</code> keep forever for backward compatibility with JSS {.unnumbered}


### Description

alias to <code>get_draws()</code> keep forever for backward compatibility with JSS



### Usage

<pre><code class='language-R'>posterior_draws(x, shape = "long")
</code></pre>

